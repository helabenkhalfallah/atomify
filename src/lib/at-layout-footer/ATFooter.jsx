import React from 'react';
import Layout from 'antd/es/layout';
import 'antd/es/layout/style';

const {
  Footer,
} = Layout;

const ATFooter = (props) => <Footer {...props} />;

export default ATFooter;
