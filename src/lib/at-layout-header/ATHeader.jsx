import React from 'react';
import Layout from 'antd/es/layout';
import 'antd/es/layout/style';

const {
  Header,
} = Layout;

const ATHeader = (props) => <Header {...props} />;

export default ATHeader;
