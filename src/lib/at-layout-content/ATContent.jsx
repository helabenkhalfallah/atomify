import React from 'react';
import { Layout } from 'antd';
import 'antd/es/layout/style';

const {
  Content,
} = Layout;

const ATContent = (props) => <Content {...props} />;

export default ATContent;
